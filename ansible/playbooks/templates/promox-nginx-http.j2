server {
    listen 80;
    server_name {{ app_domain }};

    # Certbot HTTP-01 challenge — must be reachable before cert is issued
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}