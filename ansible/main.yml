---
# First-time Proxmox host setup (run once)
- import_playbook: playbooks/proxmox-jump-user.yml
- import_playbook: playbooks/proxmox-nginx.yml

# LXC configuration (run by GitHub Actions on each deploy)
- import_playbook: playbooks/lxc-configure.yml




